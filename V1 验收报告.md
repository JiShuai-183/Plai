# Plai V1 验收报告

- 验收日期：2026-08-31
- 验收对象：`D:\Study\Project\Plai`（Flutter 3.47.2 / Dart 3.13，Riverpod 2.6，sqflite 纯本地）
- 验收方式：集成评审子代理全量验收（一致性检查 → 接口契约 → 集成联调 → 补充测试 → 代码审查 → 构建验证）

---

## 1. 验收结论

> **有条件通过 ✅**

- 7 份 PRD 中 **V1 范围**（课表 + 日程 + 提醒 + 备份恢复 + 设置，见 PRD-总览/README 版本路线图）功能全部实现，数据字段与接口契约核对一致。
- 集成联调中发现 **4 个真实集成缺陷**，已全部修复并新增回归测试验证（详见 §5、§8）。
- 验证门槛全绿：`flutter analyze` 无 error / `flutter test` 93 通过 / `flutter build apk --debug` 出包成功。
- **通过条件（发版前必做）**：当前仅能出 debug 包，**release 签名未配置**（`android/app/build.gradle.kts` 为占位签名，见 §9.1）。配置签名后即可正式发版；若本期以 debug 包自用/演示，则条件已满足。

---

## 2. 验收范围与依据

- 依据文档：`E:\Obsidian 仓库\Plai开发\` 下 7 份 PRD + `编码约定.md` + `数据层接口文档.md` + `提醒调度接口.md`。
- **V1 范围界定**（README「版本路线图」）：
  - **V1**：课表（手写录入 + 文件导入导出 + 单双周/周序/节假日）+ 日程（手动创建 + 完成打卡）+ 本地通知提醒 + 备份恢复，安卓，完全离线可用。
  - **V2**：积分激励 + AI 模块（对话 / 文本批量生成 / OCR 识别课表）。**明确不在本期验收范围**（PRD 均标注「V2 实现」），本期不做对应实现属正常，不构成缺陷。

---

## 3. PRD 覆盖核对表

| PRD | 条目 | 状态 | 实现落点 |
|---|---|---|---|
| PRD-总览 / README | V1 范围三 Tab 骨架（课表/今日/设置） | ✅ | `app_shell.dart`（IndexedStack）、`main.dart` |
| | 纯本地、零服务器 | ✅ | 全库仅 SQLite（`data/db`）+ 本地通知 |
| | 命名路由统一、禁止散落字符串 | ✅ | `routes/app_routes.dart` + `route_registry.dart`（9 路由全登记） |
| PRD-课表模块 | 学期管理（增删改/最新学期/当前切换） | ✅ | `semester_page.dart`、`currentSemesterProvider` |
| | 课程 CRUD（颜色/单双周/自定义周序/星期/节次/教师/地点） | ✅ | `course_form_page.dart`、`data/models/course.dart` |
| | 周次规则引擎（weekOfDate/hasClass/isCourseHoliday/clampWeek/weekDate） | ✅ | `week_rules.dart`（纯逻辑，单测覆盖） |
| | 周视图（撞课多列/当前周定位/周切换） | ✅ | `timetable_week_page.dart` + `class_lanes.dart` + `week_view.dart` |
| | 日视图 | ✅ | `day_view_page.dart` |
| | 停课/节假日 | ✅ | `holidaysProvider` + 停课 CRUD（数据层接口） |
| | 文件导入导出（JSON/CSV、覆盖/合并/新建学期、严格行号校验） | ✅ | `import_export_page.dart` + `data/import_export/timetable_import_export.dart` |
| | 节次时间表 | ✅ | `period_page.dart` |
| PRD-日程模块 | 任务 CRUD（优先级/截止/关联课程等字段） | ✅ | `task_form_page.dart`、`data/models/task.dart` |
| | 完成打卡（完成/取消，联动提醒） | ✅ | `toggleTaskCompleted`（`schedule_providers.dart`） |
| | 任务列表（今日/逾期/已完成分组） | ✅ | `task_list_page.dart`、`schedule_page.dart` |
| | 日历视图 | ✅ | `calendar_page.dart` |
| | 任务详情/删除确认 | ✅ | `task_detail_page.dart`、`task_actions.dart` |
| 提醒（总览 V1 范围） | 本地通知、上课提醒（周次/停课过滤、提前量） | ✅ | `notification_service.dart`、`notification_scheduler.dart` |
| | 任务提醒 | ✅ | `scheduleTaskReminder` |
| | 通知 ID 分区（课程/任务） | ✅ | `notification_ids.dart`（课 `1_000_000+cid*128+week`，任务 `2_000_000+tid`） |
| | 深链跳转（冷启动/运行中，未登记兜底根路由） | ✅ | `NotificationService._dispatchIntent` + `routeRegistry.containsKey` |
| | 全量重排（课程/节次/学期/设置/恢复改动后） | ✅ | `rescheduleTimetableReminders` / `rescheduleAll` |
| | 保活引导（首次开启提醒） | ✅ | `keep_alive_guide_page.dart`（plai-notify 拥有） |
| PRD-设置与数据 | SQLite 本地存储（schema/迁移） | ✅ | `app_database.dart`、`db_schema.dart` |
| | 设置项（通知开关/上课提前量/主题） | ✅ | `settings_page.dart`、`theme_controller.dart`、`settings_providers.dart` |
| | 备份/恢复（.plai，合并/覆盖 + 强确认） | ✅ | `backup_page.dart`、`data/backup/backup_service.dart` |
| | 节次模板（内置默认/自定义） | ✅ | `periods_edit_page.dart`、`default_periods.dart` |
| | 权限清单 | ✅ | `android/app/src/main/AndroidManifest.xml` |
| PRD-AI模块 | 对话/文本导入/OCR 等能力 | ⏳ V2 | 本期无实现（PRD 标注 V2），建议 V2 启动 |
| PRD-积分激励 | 积分规则/日历颜色/成就音效 | ⏳ V2 | 本期无实现（规则已定稿），建议 V2 启动 |

---

## 4. 接口契约核对结果

- **依赖方向合规**：所有 feature 页面/Provider 只读取 `data/repositories` 的接口（`ITimetableRepository` / `ITaskRepository` / `ISettingsRepository` / `TimetableImportExport` / `BackupService`），**未发现任何 feature 直接写 SQLite**。
- **路由登记齐全**：`route_registry.dart` 中 `root / timetable / today / settings / keepAliveGuide / periodsEdit / backup / timetableWeek / taskDetail` 全部登记；`NotificationService` 深链分派先查 `routeRegistry.containsKey`，未登记则回退根路由（`timetableWeek`/`taskDetail` 均已登记，深链可达）。
- **提醒调度契约**：课表模块用自己的 `WeekRules` 算出计划（`buildClassReminderPlans`）传入 `rescheduleAll(classPlans: ...)`，符合《提醒调度接口.md》「课表模块自己算好哪几周有课、跳过停课周」的约定。

---

## 5. 集成联调：发现并修复的缺陷（4 处）

| # | 缺陷 | 影响 | 修复 |
|---|---|---|---|
| 1 | **CSV 导入 UI 总是调 `importJson`**（`import_export_page.dart` 的 switch 未按文件类型分派） | 从 UI 选 CSV 文件导入必失败 | switch 内按 `isJson` 分派到 `importCsv`，CSV 目标学期固定为当前学期（页面已先保证非空且有主键） |
| 2 | **`todayCoursesProvider` 在学期外也返回课程** | 开学前/结课后「今日课程」误把未来/历史周的课标成今天 | 学期外（今日 < 开学日 或 > 结课日）直接返回空 |
| 3 | **两个提醒展开器未按学期总周数封顶**（`buildClassReminderPlans` 与 `ClassReminderPlanner.expand`） | 导入数据允许 endWeek 到 99，可能为超学期周生成无效提醒 | 循环上界收敛到 `min(course.endWeek, semester.totalWeeks)` |
| 4 | **备份恢复后未失效 Riverpod 缓存** | 恢复课表/日程/主题后各 Tab 仍显示旧数据，直到重启 | 恢复后 `ref.invalidate` 主题/学期/课程/节次/停课/提前量/任务/今日视图等全部相关 Provider，并全量重排提醒 |

---

## 6. 代码审查结论

- **错误处理**：导入/导出/备份/恢复均有 try-catch 与用户提示；通知重排失败不阻断主流程（`_safeReschedule`、`rescheduleTimetableReminders` 内 catch）。
- **空态**：无学期（课表页提示创建）、无节次（提示恢复内置模板）、今日无课、任务空态均有兜底文案。
- **删除确认**：任务删除、学期删除有确认；备份「覆盖恢复」要求二次输入「覆盖」强确认。
- **边界**：课程表单校验 `endWeek ≤ totalWeeks`；周次引擎 `clampWeek`；导入严格校验（非法行报行号、整体失败不部分写入）。
- **遗留设计异味（不影响验收，见 §9）**：同一 `settingsRepositoryProvider` 在 `timetable_providers.dart` 与 `settings_providers.dart` 各定义一份；节次编辑存在两个等价页面。

---

## 7. 测试与构建结果

| 项 | 结果 |
|---|---|
| `flutter analyze` | ✅ 无 issue（约 1.3s） |
| `flutter test` | ✅ **93/93 通过**（原 83 + 新增 10） |
| `flutter build apk --debug` | ✅ 成功出包 `build\app\outputs\flutter-apk\app-debug.apk`（约 188 MB，含全 ABIs） |

**本次新增测试（10 条）**：
- `test/timetable/reminder_planner_test.dart`（7 条）：`buildClassReminderPlans` 的每周/单周展开、日期时刻正确性、**结束周超出学期总周数封顶**（缺陷 3）、开始周越界、停课跳过、节次缺失跳过、from 过期过滤。
- `test/schedule/today_courses_provider_test.dart`（3 条）：`todayCoursesProvider` 学期内正常展示、**开学前/结课后返回空**（缺陷 2），用真实 in-memory 库 + `ProviderContainer` 覆盖仓库实现。

> CSV 导入 UI 分派（缺陷 1）为 UI 层逻辑，依赖 `file_picker` 无法在 widget 测试中直接触发；数据层 `importCsv` 本身已有 `test/data/timetable_import_export_test.dart` 覆盖（4 条），UI 分派修复经人工代码审查 + `flutter analyze` 确认。

---

## 8. 修复文件清单

**本次验收修改（5 个源码文件 + 2 个新增测试文件）：**
- `lib/features/timetable/import_export_page.dart` — 缺陷 1（CSV 导入分派）
- `lib/features/schedule/schedule_providers.dart` — 缺陷 2（今日课程学期外过滤）
- `lib/features/timetable/reminder_planner.dart` — 缺陷 3（周次封顶）
- `lib/services/notifications/class_reminder_planner.dart` — 缺陷 3（周次封顶）
- `lib/features/settings/backup_page.dart` — 缺陷 4（恢复后 Provider 失效）
- `test/timetable/reminder_planner_test.dart`（新增）
- `test/schedule/today_courses_provider_test.dart`（新增）

**未改动（按要求保留）：** `android/gradle/wrapper/gradle-wrapper.properties`（腾讯镜像）、`android/gradle.properties`（`kotlin.incremental=false`）、`android/app/build.gradle.kts`（desugaring + `desugar_jdk_libs:2.1.5`）。以上三项为工作区未提交改动，保留在验收态。

> 注：本次修改与新增测试目前为**未提交**状态（git 已有 2 次提交）。建议用户自行 `git add` 上述 lib/test 文件后提交；android 三项配置是否一并提交由用户决定。

---

## 9. 已知问题

1. **【发版阻塞】release 签名未配置**：`android/app/build.gradle.kts` 使用 debug 占位签名（含 `TODO` 标记），`flutter build apk --release` 不可用于分发。发版前需配置正式 keystore 与签名。
2. **`settingsRepositoryProvider` 重复定义**：同一 Provider 在 `timetable_providers.dart` 和 `settings_providers.dart` 各定义一份（`backup_page.dart` 用 `hide settingsRepositoryProvider` 规避二义）。功能无影响，建议下一阶段合并到一处。
3. **节次编辑入口冗余**：`timetable/period_page.dart`（`PeriodManagePage`，从课表页进）与 `settings/periods_edit_page.dart`（`PeriodsEditPage`，从设置页路由进）是同表两个功能等价页面，维护成本翻倍。建议合并其一。
4. **debug 包体积 188 MB**：含全 ABI + 调试符号。发版时 release + ABI 裁剪（`splits`/`abiFilters`）可显著缩小。

---

## 10. 建议下一阶段事项

1. **发版准备**：配置 release 签名（keystore + `signingConfigs`），验证 `flutter build apk --release` 出包，开启 ABI 裁剪。
2. **代码清理**：合并重复 `settingsRepositoryProvider`；合并两个节次编辑页；按需把冗余项清理后补一次回归测试。
3. **V2 启动（可并行）**：积分激励（规则已定稿，规则引擎可先行单测）+ AI 模块（多供应商 OpenAI 兼容协议，用户自填 Key）。README/PRD 目录中的「用户评审 PRD」里程碑可推进。
4. **补充测试（可选）**：为备份覆盖/合并恢复、通知 ID 分区、任务提醒重排补更多端到端断言；widget 冒烟测试可扩到课表周视图/日历页。

---

## 11. 验收命令与结果附录

```bash
cd D:/Study/Project/Plai
flutter analyze                     # No issues found! (1.3s)
flutter test                        # 00:02 +93: All tests passed!
flutter build apk --debug           # √ Built build\app\outputs\flutter-apk\app-debug.apk
```
