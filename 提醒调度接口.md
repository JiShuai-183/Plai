# Plai 提醒调度接口文档

> 版本：V1（2026-08-31）· 归属：`lib/services/notifications/**`（plai-notify）
> 关联文档：《编码约定.md》《数据层接口文档.md》
> 适用对象：timetable（课表）/ schedule（日程）/ settings（设置）模块 —— **只读引用本文档与本模块的公开接口**，不要直接操作 `flutter_local_notifications` 插件。

---

## 1. 定位与边界

- 本模块负责：**本地通知与提醒调度**（上课提醒 / 任务提醒）、权限申请（Android 13+ 通知授权、精确闹钟）、国内 ROM 保活引导、提醒全量重排。
- 技术栈：`flutter_local_notifications ^22.3.0` + `timezone ^0.11.1`（作为直接依赖加入）。
- 关键边界（由主会话拍板）：
  - **调度接口一律接收明确的具体日期**，不接收「周次规则」。哪几周有课由课表模块（timetable）自己算好，逐次调用 `scheduleClassReminder`。
  - **本模块不实现 / 不依赖 `weekOfDate` / `hasClass`**（那是 timetable 的职责），避免循环依赖。`rescheduleAll` 内部展开课程实例的兜底逻辑见 §8。
  - 数据读写仅通过 `lib/data/` 的 Repository 接口（`ITimetableRepository` / `ITaskRepository` / `ISettingsRepository`），不直接写 SQLite。

---

## 2. 目录结构

```
lib/services/notifications/
  notification_service.dart      # NotificationService：初始化/渠道/权限/深链分发（单例）
  notification_scheduler.dart    # NotificationScheduler：调度接口 + 设置键常量（timetable/schedule/settings 主入口）
  notification_payload.dart      # 通知 payload 编解码 + NotificationIntent
  notification_ids.dart          # 通知 ID 分区 + 通知渠道常量
  class_reminder_planner.dart    # 课程→具体提醒计划展开（rescheduleAll 兜底用，纯逻辑）
  navigator.dart                 # appNavigatorKey 全局导航 key
  keep_alive_guide_page.dart     # 「提醒保护」页（KeepAliveGuidePage：保活检测 + 测试提醒，本模块拥有）
  keep_alive_checker.dart        # 保活检测层（KeepAliveChecker：三态检测 / 跳系统设置 / 手动确认 / 品牌持久化）
  notification_providers.dart    # notificationSchedulerProvider / notificationServiceProvider / keepAliveCheckerProvider（Riverpod）
test/notifications/              # 纯逻辑单测（payload / id / planner / keep_alive_checker / 保活页 widget）
```

---

## 3. 核心概念

### 3.1 通知 ID 分区（稳定可覆盖）

同一「课程+周」/「任务」重复调度时使用**相同 ID**，新通知覆盖旧通知，天然实现「数据变更后重排」：

| 类型 | 规则 | 示例 |
|---|---|---|
| 上课提醒 | `1_000_000 + courseId*128 + week` | 课程 12、第 5 周 → `1000613` |
| 任务提醒 | `2_000_000 + taskId` | 任务 3 → `2000003` |
| 测试提醒（提醒保护页） | 固定 `NotificationIds.testReminderId = 999999` | `999999` |

（`week` 编码进低 7 位，周期上限 99 满足 PRD。）

`testReminderId` 刻意取 `999999`：低于上课提醒下界 `1_000_000`（`classReminderId` 最小值为 `1_000_000 + 1*128 + 1 = 1000129`）、远低于任务提醒下界 `2_000_000`，三者分区互不冲突 —— 测试提醒不会被业务提醒覆盖，反之亦然。

### 3.2 通知渠道

默认渠道 `plai_reminders`（名称「上课与任务提醒」），`importance=high`，通知音走**系统默认**（不指定自定义 sound）。

### 3.3 设置键（与设置模块共享，字面量不可改）

| 常量（`NotificationSettingsKeys`） | 键字面量 | 说明 |
|---|---|---|
| `enabled` | `notify.enabled` | 通知总开关，`'true'`/`'false'`，缺省视为开启 |
| `classAdvanceMin` | `notify.class_advance_min` | 上课提醒提前量（分钟），如 `'10'`；缺省兜底 `10` |
| `keepAliveGuideShown` | `notify.keep_alive_guide_shown` | 保活引导页是否已展示，`'true'`/`'false'` |

**保活检测相关键**（常量定义在 `KeepAliveChecker`，不在 `NotificationSettingsKeys`；调度逻辑不读）：

| 常量（`KeepAliveChecker`） | 键字面量 | 说明 |
|---|---|---|
| `brandKey` | `notify.keepalive_brand` | 用户上次选择的品牌（存 `KeepAliveBrand.name`，如 `xiaomi`）。页面首次进入按 `Build.MANUFACTURER` 自动预选后，用户手改的品牌记在这里，下次优先用它 |
| `manualConfirmPrefix` | `notify.keepalive_confirmed.<itemId>` | 「我已完成」手动确认，值 `'true'`/`'false'`。仅对无法自动检测的项（当前只有 `auto_start`）写入 |

> `notify.keepalive_confirmed.*`：页面进入时从这些键恢复「我已完成」，跨退出再进保留；**每次点「检测」会先整体清空**（避免上一次的陈旧 ✓ 骗人），随后把本次仍有展示的已确认项重新落库。详见 §7.2。

---

## 4. 调度接口（主契约）

入口：`notificationSchedulerProvider`（`lib/services/notifications/notification_providers.dart`）。

```dart
final notificationSchedulerProvider = Provider<NotificationScheduler>((ref) { ... });

// 使用方式
final scheduler = ref.read(notificationSchedulerProvider);
```

### 4.1 上课提醒（timetable 调用）

```dart
Future<void> scheduleClassReminder({
  required Course course,      // 课程
  required DateTime date,      // 上课日期（仅年月日，具体到天）
  required TimeOfDay time,     // 上课时刻（节次开始时间）
  required int advanceMin,     // 提前提醒分钟数（≥0，来自设置）
  required int week,           // 第几周（通知 ID 稳定 + 点击通知定位课表周）
})
```

- 触发时刻 = `date + time - advanceMin`；**已过期自动跳过**。
- **`week` 必须传**：通知 ID 按「课程+周」稳定、深链定位课表周都需要它；课表模块展开实例时本来就持有周次。
- 同一课程同一周重复调用 = 覆盖旧通知。

### 4.2 任务提醒（schedule 调用）

```dart
Future<void> scheduleTaskReminder(Task task)
```

- 触发时刻优先级：`task.remindDate`（冗余存储）> `dueDate + dueTime - remindOffsetMin`（`-1`=准时，`null`=不提醒）。
- 已完成 / 不提醒 / 已过期 → 取消对应通知（保证重排一致性）。

### 4.3 取消（数据变更 / 开关切换）

```dart
Future<void> cancelAllRemindersFor({int? courseId, int? taskId})  // 取消某课程全部周 / 某任务
Future<void> cancelAll()                                            // 取消全部提醒（不清除渠道）
```

- `cancelAllRemindersFor` 通过遍历「待触发通知」按 payload 匹配，**不依赖周次推算**。

### 4.4 全量重排（settings / 数据变更调用）

```dart
Future<void> rescheduleAll({List<ClassReminderPlan>? classPlans})
```

- 流程：`cancelAll()` → 读设置（总开关、提前量）→ 重排任务 → 重排课程。
- 总开关关闭（`notify.enabled == 'false'`）→ 只取消不重排。
- `classPlans` 可传入课表模块算好的具体计划（`List<ClassReminderPlan>`，见 §8），跳过内部展开；缺省时按学期 / 节次 / 停课数据内部展开（含节次缺失、停课、过期过滤）。

### 4.5 保活引导辅助（settings 调用）

```dart
Future<bool> shouldShowKeepAliveGuide()   // 首次开启提醒时是否需展示引导页
Future<void> markKeepAliveGuideShown()     // 标记已展示（保活引导页打开时也会自动标记）
```

### 4.6 测试提醒（提醒保护页，只读）

```dart
// 触发一条测试提醒（约 10 秒后触发），供用户自证渠道 / 提示音是否正常，
// 以及「App 未启动也能响」。走真实调度链路（复用 _schedule 的精确调度 +
// 降级链路）。payload 为空串，NotificationPayload.parse('') == null，
// 点击不会误跳页面。
Future<void> scheduleTestReminder()
```

- 展示位置：「提醒保护」页（`KeepAliveGuidePage`，路由 `AppRoutes.keepAliveGuide`）底部的
  「发一条测试提醒」按钮；页面引导用户「①留 App 内点一次、②点后立刻划掉 App」两步自证。
- **不参与业务调度**、不影响任何提醒的排布；测试提醒用固定 ID
  `NotificationIds.testReminderId`，与上课 / 任务提醒分区不冲突（见 §3.1）。

---

## 5. 权限申请（settings 首次使用提醒时调用）

```dart
final service = ref.read(notificationServiceProvider); // = NotificationService.instance
await service.requestPermissions();        // 返回通知权限是否授予（bool）
await service.areNotificationsEnabled();   // 通知权限是否已授予
await service.canScheduleExactAlarms();    // 是否可精确调度（SCHEDULE_EXACT_ALARM）
```

行为：
- **Android 13+**：弹 `POST_NOTIFICATIONS` 系统授权框（首次使用提醒时）。
- **Android 12+**：`SCHEDULE_EXACT_ALARM` 拉起系统设置页引导授权；同时声明了 `USE_EXACT_ALARM`（Android 14+）时系统默认授予，该调用为 no-op。未授予时调度自动降级为非精确（见 §9 遗留）。
- **iOS / macOS**：`requestPermissions(alert/badge/sound)`。

**调用方（settings 模块）职责**：首次开启提醒时，先 `requestPermissions()`，再用 `shouldShowKeepAliveGuide()` 判断是否导航到保活引导页；开启后建议调用 `rescheduleAll()` 立即生效。

---

## 6. 通知点击深链（路由契约）

点击通知 → 打开 App 并定位到对应页面。冷启动（App 未运行）与运行中点击均支持，由 `NotificationService` 统一分发。

### 6.1 路由名常量（`AppRoutes`，`lib/routes/app_routes.dart`）

| 常量 | 路由 | 参数（arguments） | 登记方 | 目标 |
|---|---|---|---|---|
| `AppRoutes.timetableWeek` | `/timetable/week` | `int` 周次 | **plai-timetable**（待登记） | 定位到课表某周 |
| `AppRoutes.taskDetail` | `/task/detail` | `int` 任务主键 | **plai-schedule**（待登记） | 定位到任务详情 |
| `AppRoutes.keepAliveGuide` | `/settings/keep-alive-guide` | 无 | **plai-notify**（已登记） | 「提醒保护」页（保活检测 + 测试提醒） |

> 注意：`timetableWeek` / `taskDetail` 两个路由**由课表/日程模块登记**（登记到 `lib/routes/route_registry.dart`）。在登记之前，通知深链会自动回退到根路由，不会崩溃。登记后深链即刻生效，本模块无需改动。
>
> `notificationDiagnostics` 路由常量与注册项已移除（原自检页内容先并入保活页，后随「提醒不响」问题定位完成一并删除）。
> **`keepAliveGuide` 的路由名与路径 `/settings/keep-alive-guide` 刻意保留未改** —— 页面标题虽已从「提醒保活引导」改为「提醒保护」，但改动路由名没有任何收益、只会牵动 scaffold 与既有跳转，故沿用旧名。**这不是漏改。**

### 6.2 payload 格式（本模块定义，不可变契约）

| 类型 | payload JSON | 说明 |
|---|---|---|
| 上课提醒 | `{"type":"class","courseId":12,"week":5}` | 点击 → `timetableWeek`，参数 `5` |
| 任务提醒 | `{"type":"task","taskId":3}` | 点击 → `taskDetail`，参数 `3` |

### 6.3 分发机制

- 冷启动：`main()` 预初始化服务 → `getNotificationAppLaunchDetails()` 读到启动通知 → 首帧后 `appNavigatorKey.currentState.pushNamed(...)`。
- 运行中：`onDidReceiveNotificationResponse` → 同一分发函数。
- 目标路由未登记 → 回退 `AppRoutes.root`。

---

## 7. 「提醒保护」页（原保活引导，plai-notify 拥有）

### 7.1 页面形态

- 页面：`KeepAliveGuidePage`（AppBar 标题「提醒保护」），路由 `AppRoutes.keepAliveGuide`（路由名与路径刻意未改，见 §6.1 注）。
  打开页面即自动标记 `keepAliveGuideShown`；设置模块在首次开启提醒时：`shouldShowKeepAliveGuide()` 为 true → `Navigator.pushNamed(context, AppRoutes.keepAliveGuide)`。
- 页面已由「只读文字步骤」改造为 **型号自动预选 + 一键检测 + 直达系统设置页**，并提供测试提醒入口（见 §4.6）。

| 区块 | 行为 |
|---|---|
| 品牌选择 | 六个 `ChoiceChip`（小米/红米 / 华为 / 荣耀 / OPPO / vivo / 通用）。进入时按 `Build.MANUFACTURER` **自动预选**（`KeepAliveBrand.fromManufacturer`，大小写不敏感；`xiaomi\|redmi\|poco`→xiaomi、`huawei`→huawei、`honor\|hihonor`→honor、`oppo\|oneplus\|realme`→oppo、`vivo\|iqoo`→vivo、其余→generic）。用户可手改，选择持久化到 `notify.keepalive_brand`，下次进页面优先用它 |
| 「检测当前设置」 | 调 `KeepAliveChecker.collectChecks(brand)` 逐项渲染；**任何一步失败降级 unknown，绝不抛、不白屏** |
| 检测项集合 | xiaomi/huawei/honor/oppo/vivo：自启动 + 电池优化 + 通知权限 + 精确闹钟；generic：后三项（不含自启动） |
| 结果三态 | `ok` → ✓「已完成」；`notOk` → 「点击设置」+ 后果说明；`unknown` → 「点击设置」+「无法自动检测，请自行确认」+「我已完成」勾选框 |
| 完整步骤 | 各品牌旧版文字步骤**逐字保留**，收在「<品牌> 完整步骤」可展开区（跳转后仍可能找不到入口，文字是兜底）；检测前默认展开，检测后收起 |
| 测试提醒 | 「发一条测试提醒（10 秒后）」按钮 + 两步自证引导（留 App 内点一次 → 划掉 App 再点一次），验证渠道 / 提示音 / 未启动也能响（见 §4.6） |
| 反馈 | 统一 `showPlaiToast` 气泡，不用 SnackBar |

### 7.2 手动确认（`manualConfirmable`）

仅无法自动检测的项需手动确认（当前只有 `auto_start`，见 `KeepAliveChecker.manualConfirmableIds`）。

- 确认值存 `notify.keepalive_confirmed.<itemId>`（`'true'`/`'false'`）。
- **页面进入时会从设置恢复**这些确认，勾选跨「退出再进」保留。
- **每次点「检测」先清空这些键**（避免上一次的陈旧 ✓ 骗人）；随后把本次仍有展示的已确认项**重新落库**，所以「检测 → 勾选 → 退出 → 再进 → 检测」勾选仍在。

### 7.3 原生通道 `plai/keep_alive`（`MainActivity.kt`）

Dart 侧入口：`KeepAliveChecker`（`keep_alive_checker.dart`）→ Riverpod `keepAliveCheckerProvider`。

| 方法 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `getManufacturer` | 无 | `String` | `Build.MANUFACTURER`，用于品牌预选；失败返回空串 → generic |
| `checkBatteryOptimization` | 无 | `Boolean` | `PowerManager.isIgnoringBatteryOptimizations(packageName)`，标准 API，结果准确 |
| `checkAutoStart` | 无 | `"allowed"` / `"denied"` / `"unknown"` | 见下方「尽力而为」说明 |
| `openSettings` | `target` | `"opened"` / `"failed"` | 见下方跳转说明 |

`openSettings` 的 `target` 取值：`notification`（通知设置）/ `exactAlarm`（精确闹钟）/ `battery`（电池优化豁免，失败回退电池优化列表页）/ `autostart`（厂商自启动管理页）/ `appDetails`（应用详情，未识别 target 的兜底）。统一 `startActivity(intent.addFlags(FLAG_ACTIVITY_NEW_TASK))`，整体 try/catch，**绝不崩**。

**自启动检测是「尽力而为」，刻意不给假 ✓（重要）**：

- 「自启动」在 Android 上**没有任何标准 API**。只能反射 MIUI 私有 AppOps op，而隐藏 API 反射常被系统拦截。**因此本方法在结构上很可能只返回 `unknown`** —— 这是刻意降级，不是 bug。
- 三态语义（严格）：
  - `"allowed"` ← **仅当**可信 op 明确返回 `MODE_ALLOWED`。
  - `"denied"` ← **仅当**可信 op 明确返回 `MODE_IGNORED`；**`denied` 只可能来自语义明确的可信 op，别无来源**。
  - `"unknown"` ← 其余**一切**（拿不到字段 / 反射被隐藏 API 策略拦 / 查询抛异常 / `MODE_DEFAULT` / `MODE_ERRORED`）。
- **`MODE_ERRORED` 不算 `denied`**：它的语义是「查询本身出错 / op 不存在 / 无权限」，**不是「用户关闭了」**。只有 `MODE_IGNORED` 才是真正的「用户关闭」。曾有版本把 `MODE_ERRORED` 也判为 `denied`，于「用未经验证的 op 去查 → 系统回 `MODE_ERRORED` → 判未开启」，正是真机「明明开了却报未开启」的假 negative 来源。
- **已删除社区字面量候选 `10008` / `10021`**：它们是**猜的** op，语义未经证实，用未知 op 得到的 mode 没有解释力；真机已证明会产生假 `denied`（用户看到 App 说没开、实际开了，摧毁信任）。**宁可不检测（`unknown`），也不给错答案。**
- 可信组取值**尽力而为**：`getField` 与 `getDeclaredField`（配 `setAccessible(true)`）都试；后者可命中隐藏字段，但隐藏 API 黑名单仍可能拦截 —— 拦住了就老实返回 `unknown`。**绝不为凑一个答案而放宽判断。**
- 跳转同理**不赌单一组件名**：按候选清单（小米 → 华为 → 荣耀 → OPPO（coloros/oppo 两个）→ vivo）逐个 `resolveActivity` 判可解析后再启动，**全部失败回退应用详情页**。候选组件名随 ROM 版本可能失效，需真机校准（见 §11）。

---

## 8. 上课提醒实例展开（仅供 rescheduleAll 兜底）

`ClassReminderPlanner.expand(...)` 纯逻辑：
- 输入：`Course`、`Semester`、`List<Period>`、`List<Holiday>`、`advanceMin`、可选 `from`。
- 周次过滤：`weekType`（every/odd/even/custom+weekList）作用于 `startWeek..endWeek`。
- 日期推算：学期开学日 + `(week-1)*7` + 星期偏移。
- 停课过滤：全局（`courseId == null`）或该课程的 `Holiday` 命中则跳过。
- 过期过滤：早于 `from`（默认 now）的计划不产出；节次表缺起始节次 → 跳过该课程。

课表模块若已有规范周次引擎，可自行算好 `List<ClassReminderPlan>` 传给 `rescheduleAll`，本模块不重复展开。

---

## 9. Android 配置清单（Phase 4 构建需要，已配置）

见 `android/app/src/main/AndroidManifest.xml`：

- 权限：
  - `android.permission.POST_NOTIFICATIONS`
  - `android.permission.RECEIVE_BOOT_COMPLETED`
  - `android.permission.SCHEDULE_EXACT_ALARM`
  - `android.permission.USE_EXACT_ALARM`
  - `android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`（「提醒保护」页电池优化项 `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 直弹授权框的前提；受 Google Play 政策限制，但本项目侧载分发、不上架，无影响）
- 包可见性 `<queries>`（Android 11+）：除既有的 `PROCESS_TEXT`，另声明六个厂商自启动管理包
  （`com.miui.securitycenter` / `com.huawei.systemmanager` / `com.hihonor.systemmanager` /
  `com.coloros.safecenter` / `com.oppo.safe` / `com.vivo.permissionmanager`）。
  **不加则 `resolveActivity` 恒为 null，「一键去自启动设置」全盘失效**（只能回退应用详情页）。
- 原生通道 `plai/keep_alive`：在 `MainActivity.configureFlutterEngine` 内 inline 注册（与 `plai/app_update` 同风格），四个方法见 §7.3。
- 接收器（插件必需，`com.dexterous.flutterlocalnotifications`）：
  - `ScheduledNotificationReceiver`（exported=false）
  - `ScheduledNotificationBootReceiver`（exported=false，含 `BOOT_COMPLETED` / `MY_PACKAGE_REPLACED` / `QUICKBOOT_POWERON` / `com.htc.intent.action.QUICKBOOT_POWERON` intent-filter，开机后恢复调度）
  - `ActionBroadcastReceiver`（exported=false，通知操作按钮回调）
- 通知小图标：`res/drawable/ic_notification.xml`（引用启动器图标，作为插件初始化默认图标 `AndroidInitializationSettings('ic_notification')`）。

> 说明：v22 插件的 `ScheduledNotificationReceiver` 由插件以显式 Intent 调用，**无需** `SCHEDULE_NOTIFICATION` intent-filter（该写法属于旧版本），以 v22 官方 README 为准。

---

## 10. 调用示例（按模块）

### timetable（课表模块）——上课提醒

```dart
// 课表模块自算「本周有课」并得到 (week, date, startTime)，逐次调用：
await scheduler.scheduleClassReminder(
  course: course,
  date: date,                 // 如 DateTime(2026, 9, 2)
  time: const TimeOfDay(hour: 8, minute: 0),
  advanceMin: 10,
  week: week,                 // 如 1
);

// 课程删除 / 编辑后：
await scheduler.cancelAllRemindersFor(courseId: courseId);
await scheduler.rescheduleAll(); // 或只重排该课程所在学期
```

### schedule（日程模块）——任务提醒

```dart
await scheduler.scheduleTaskReminder(task);   // 保存任务后调用
await scheduler.cancelAllRemindersFor(taskId: taskId);  // 删除任务时调用
```

### settings（设置模块）——开关与引导

```dart
// 开启通知：
final granted = await service.requestPermissions();
if (granted && await scheduler.shouldShowKeepAliveGuide()) {
  Navigator.pushNamed(context, AppRoutes.keepAliveGuide);
}
await scheduler.rescheduleAll();   // 生效

// 关闭通知：
await scheduler.cancelAll();
```

---

## 11. 遗留问题（真机验证项）

1. **真机通知展示 / 点击深链 / 开机恢复未验证**：本机无 Android SDK，`flutter analyze` + 单测通过，但需真机（Android 8~14）验证：
   - 上课 / 任务提醒按时弹出，提示音为系统默认；
   - 冷启动点击通知 → 定位课表周 / 任务详情（需 timetable/schedule 登记路由后）；
   - 精确闹钟授权弹窗与未授权时的非精确降级；
   - 重启手机后 `ScheduledNotificationBootReceiver` 恢复已调度通知；
   - 各品牌 ROM 保活路径实际可达性。
2. **通知小图标**：当前复用启动器图标（彩色），Android 5+ 渲染为白色剪影，可能不美观；生产发布前建议替换为专用单色通知图标（`res/drawable/ic_notification`）。
3. **精确调度降级**：`SCHEDULE_EXACT_ALARM` 未授权时，调度内部自动降级为非精确（时间有偏差）。如需「必须精确」，settings 模块可据 `canScheduleExactAlarms()` 提示用户去系统授权。
   > 定位「提醒不响」时曾加过诊断埋点（降级计数 `degradedScheduleCount`、重排结果落库 `notify.last_reschedule_*`、「提醒保护」页的诊断展示区），**已随问题解决移除**，降级行为本身不变。
4. **时区**：固定 `Asia/Shanghai`（单次精确调度按绝对时间戳，不受影响）；如需支持海外用户可改为跟随系统时区。
5. **测试提醒 10 秒触发**：`scheduleTestReminder()` 的 10 秒延迟弹出尚未真机验证（本机无 Android 设备），需在真机「提醒保护」页点按钮确认。
6. **App 未启动时提醒是否仍触发 —— 已实测有结论**：小米/红米实测：**未开启「自启动」时，从最近任务上滑划掉 App 后提醒不再触发；开启自启动后即可触发。** 这与 force-stop 无关，是国产 ROM 的后台启动白名单行为。故「提醒保护」页把「允许自启动」列为第一优先项。要复测仍可用「两步法」：① 留 App 内点测试提醒，10 秒内应弹出；② 点后立刻划掉 App，10 秒后仍应弹出。
7. **`checkAutoStart` 在小米上的实测结论（2026-09-12）**：用户真机「已开启自启动却报未开启」—— 假 `denied`，已定位并修复两条来源（`MODE_ERRORED` 被误当 `denied`、猜的私有字面量 `10008`/`10021`）。修复后**本方法极可能恒为 `unknown`**（隐藏 API 反射被拦 / 字段名与当前 MIUI 不符），属**可接受的降级** —— 页面走「待确认 + 点击设置 + 我已完成」兜底，不会再假 ✓、也不会再假 ✗。需在真机复测确认不再出现假 `denied`。
8. **自启动候选组件名是否需按 MIUI 版本校准未验证**：候选清单（见 §7.3）为社区通用值，若某 ROM 上全部 `resolveActivity` 失败会回退应用详情页（不崩，但用户多找一步）。需真机逐个确认可达性。
