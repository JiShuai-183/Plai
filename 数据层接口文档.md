# Plai 数据层接口文档

> 版本：V1（2026-08-31）· 归属：`lib/data/**`（plai-data）
> 关联文档：《编码约定.md》《PRD-课表模块》《PRD-日程模块》《PRD-设置与数据》
> 适用对象：timetable / schedule / settings 等 feature agent —— **只读模型与 Repository 接口，禁止直接写 SQLite、禁止 import `sqflite`**（见《编码约定》§4）。

---

## 1. 定位与边界

- 数据层负责：**数据库 schema 与迁移、领域模型、Repository 接口与实现、备份恢复（.plai）、课表 JSON/CSV 导入导出**。
- 技术栈：`sqflite` 单库多表，`sqflite_common_ffi` 仅用于宿主机单元测试（dev 依赖，不进入 Android 运行时）。
- **V1 不做积分 / AI 相关表**：`point_log`、`ai_config`、`chat_session`、`chat_message` 留待 V2，通过 migration 增量添加。
- 数据流：feature 页面 → Provider → Repository 接口 → 模型。页面禁止直接操作数据库。

---

## 2. 目录结构

```
lib/data/
  db/                      # 数据库连接与迁移
    app_database.dart      #   AppDatabase 连接单例 + onCreate/onUpgrade
    db_schema.dart         #   表名常量 + 建表 DDL + 索引 DDL
    default_periods.dart   #   内置国内高校常用节次模板（12 节）
  models/                  # 领域模型（一个类一个文件）
    semester.dart          #   Semester
    course.dart            #   WeekType + Course
    period.dart            #   Period
    holiday.dart           #   Holiday
    task.dart              #   TaskType + Priority + Task
    setting_entry.dart     #   SettingEntry
    date_utils.dart        #   日期/时刻字符串工具
  repositories/            # Repository 接口与实现（feature 只依赖接口）
    timetable_repository.dart   #   ITimetableRepository + TimetableRepository
    task_repository.dart        #   ITaskRepository + TaskRepository
    settings_repository.dart    #   ISettingsRepository + SettingsRepository
  backup/                  # 备份/恢复（.plai 文件）
    backup_service.dart    #   BackupService + RestoreStrategy + BackupPreview
  import_export/           # 课表 JSON/CSV 导入导出
    timetable_import_export.dart  #   TimetableImportExport + ImportStrategy
test/data/                 # 单元测试（schema / CRUD / 导入导出 / 备份）
```

---

## 3. 数据库 Schema 与迁移

### 3.1 版本管理

- 当前版本常量 `dbVersion = 1`（`lib/data/db/app_database.dart`）。
- 打开数据库时自动 `PRAGMA foreign_keys = ON`，保证级联生效。
- 单例使用：生产 `AppDatabase.instance`；测试 `AppDatabase(factory: databaseFactoryFfi, path: inMemoryDatabasePath)`。
- `OpenDatabaseOptions.singleInstance = false`：避免测试里多个 AppDatabase 打开同一路径共享连接。

### 3.2 表结构（V1 共 6 张表）

字段名对齐 PRD；数据库列 snake_case，交换 JSON 键 camelCase。

**semester 学期**

| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 主键 |
| name | TEXT NOT NULL | 学期名（如「2026 秋」） |
| start_date | TEXT NOT NULL | 开学日期 `yyyy-MM-dd` |
| total_weeks | INTEGER NOT NULL | 总周数 |

> 结束日期 = `start_date + total_weeks*7 - 1`（模型提供只读 getter `endDate`），不入库。

**course 课程**（`semester_id → semester(id) ON DELETE CASCADE`）

| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 主键 |
| semester_id | INTEGER NOT NULL FK | 所属学期 |
| name | TEXT NOT NULL | 课程名 |
| teacher | TEXT DEFAULT '' | 教师 |
| location | TEXT DEFAULT '' | 上课地点 |
| color | TEXT DEFAULT '' | 颜色 `#RRGGBB`，空串=未设置 |
| week_type | TEXT NOT NULL | `every`/`odd`/`even`/`custom` |
| week_list | TEXT DEFAULT '[]' | 自定义周序列，JSON 数组字符串（如 `[1,3,5]`） |
| start_week | INTEGER NOT NULL | 开始周 |
| end_week | INTEGER NOT NULL | 结束周 |
| weekday | INTEGER NOT NULL | 星期（1=周一 … 7=周日） |
| start_period | INTEGER NOT NULL | 起始节次 |
| end_period | INTEGER NOT NULL | 结束节次 |

索引：`(semester_id)`、`(semester_id, weekday)`。

**period 节次**

| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 主键 |
| idx | INTEGER NOT NULL UNIQUE | 节次序号（1 起，全局唯一；列名用 `idx` 避免 SQL 关键字 `index`） |
| start_time | TEXT NOT NULL | `HH:mm` |
| end_time | TEXT NOT NULL | `HH:mm` |

**holiday 停课/节假日**（`course_id → course(id) ON DELETE CASCADE`）

| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 主键 |
| date | TEXT NOT NULL | 停课日期 `yyyy-MM-dd` |
| course_id | INTEGER NULL | 仅停某一门课时指定；NULL=当天全局停课 |
| reason | TEXT DEFAULT '' | 原因（如「国庆」） |

索引：`(date)`。

**task 日程/任务**（`course_id → course(id) ON DELETE SET NULL`）

| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 主键 |
| title | TEXT NOT NULL | 标题 |
| description | TEXT DEFAULT '' | 描述 |
| type | TEXT NOT NULL | `scheduled`（定点日程）/`todo`（待办任务） |
| due_date | TEXT NOT NULL | 截止日期 `yyyy-MM-dd` |
| due_time | TEXT NULL | `HH:mm`；定点日程必填、待办可空 |
| priority | TEXT DEFAULT 'normal' | `normal`/`important`/`urgent` |
| course_id | INTEGER NULL | 可选关联课程 |
| remind_offset_min | INTEGER NULL | 提醒偏移分钟；`-1`=准时，`NULL`=不提醒 |
| remind_date | TEXT NULL | 提醒触发时间（ISO 冗余存储，供调度） |
| completed | INTEGER DEFAULT 0 | 是否完成（0/1） |
| completed_at | TEXT NULL | 完成时间（ISO），可空 |
| created_at | TEXT NOT NULL | 创建时间（ISO） |

索引：`(due_date)`、`(completed)`。

**setting 键值设置**

| 列 | 类型 | 说明 |
|---|---|---|
| key | TEXT PRIMARY KEY | 设置键 |
| value | TEXT NOT NULL | 值（统一字符串，使用方自行解析） |

### 3.3 迁移机制

```dart
// lib/data/db/app_database.dart
onCreate: _onCreate,   // 首次建库：建全部表 + 索引（batch 原子执行）
onUpgrade: _onUpgrade, // 增量迁移，保留用户数据
```

`_onUpgrade(db, oldVersion, newVersion)` 采用「逐版本 if」：V2 新增表时在 `if (oldVersion < 2) { ... }` 里执行 `CREATE TABLE ...` 即可。**禁止删除/改名旧列**，只做增量。

---

## 4. 领域模型

所有模型提供：`toDbMap()` / `fromDbMap(Map)`（数据库行）、`toJson()` / `fromJson(Map)`（备份/导入导出，camelCase）、`copyWith(...)`、`==` / `hashCode`。

### 4.1 Semester（`semester.dart`）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int? | 主键，新建为 null |
| name | String | 学期名 |
| startDate | DateTime | 开学日期（只取年月日） |
| totalWeeks | int | 总周数 |
| endDate | DateTime（getter） | 推导的学期结束日，不入库 |

### 4.2 WeekType + Course（`course.dart`）

`enum WeekType { every, odd, even, custom }`，成员含 `code`（存储码）与 `label`（中文名）；`fromCode` 严格解析，`fromCodeLenient` 兼容中文（每周/单周/双周/自定义）。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int? | 主键 |
| semesterId | int | 所属学期 |
| name | String | 课程名 |
| teacher / location | String | 默认 '' |
| color | String | `#RRGGBB`，默认 '' |
| weekType | WeekType | 周次类型 |
| weekList | List<int> | 自定义周序列（custom 时使用） |
| startWeek / endWeek | int | 周范围 |
| weekday | int | 1=周一…7=周日 |
| startPeriod / endPeriod | int | 节次范围 |

### 4.3 Period（`period.dart`）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int? | 主键 |
| index | int | 节次序号 |
| startTime / endTime | String | `HH:mm` |

### 4.4 Holiday（`holiday.dart`）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int? | 主键 |
| date | DateTime | 停课日期 |
| courseId | int? | null=全局停课 |
| reason | String | 原因 |

### 4.5 TaskType + Priority + Task（`task.dart`）

- `enum TaskType { scheduled('scheduled','定点日程'), todo('todo','待办任务') }`
- `enum Priority { normal('normal','普通'), important('important','重要'), urgent('urgent','紧急') }`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int? | 主键 |
| title | String | 标题 |
| description | String | 描述，默认 '' |
| type | TaskType | 类型 |
| dueDate | DateTime | 截止日期 |
| dueTime | String? | `HH:mm` |
| priority | Priority | 默认 normal |
| courseId | int? | 关联课程 |
| remindOffsetMin | int? | -1 准时 / null 不提醒 |
| remindDate | DateTime? | 提醒触发时间 |
| completed | bool | 完成状态 |
| completedAt | DateTime? | 完成时间 |
| createdAt | DateTime | 构造未传时默认 now；插入时库侧兜底 |

### 4.6 SettingEntry（`setting_entry.dart`）

| 字段 | 类型 |
|---|---|
| key | String |
| value | String |

### 4.7 日期/时间约定

- 日期存 `yyyy-MM-dd`，时刻存 `HH:mm`，时间戳存 ISO8601；**全部本地时区**。
- 工具函数在 `lib/data/models/date_utils.dart`：`dateOnlyToString` / `stringToDateOnly` / `tryParseDateOnly` / `isValidTime24h`。

---

## 5. Repository 接口（feature 只依赖接口）

接口在 `lib/data/repositories/*.dart`，实现类名 `TimetableRepository` / `TaskRepository` / `SettingsRepository`，构造均接收 `AppDatabase`。实现均返回**受影响行数**（int）或**自增 id**（insert）。

### 5.1 ITimetableRepository

```dart
// ---- 学期 ----
Future<List<Semester>> getSemesters();              // 按开学日期倒序
Future<Semester?> getSemesterById(int id);
Future<int> insertSemester(Semester semester);       // 返回新 id
Future<int> updateSemester(Semester semester);
Future<int> deleteSemester(int id);                  // 级联删课程/停课；任务 courseId 置空

// ---- 课程 ----
Future<List<Course>> getCourses(int semesterId);     // 按 星期,起始节次 排序
Future<List<Course>> getAllCourses();                // 全部学期课程（备份用）
Future<List<Course>> getCoursesByWeekday(int semesterId, int weekday);
Future<Course?> getCourseById(int id);
Future<int> insertCourse(Course course);
Future<int> updateCourse(Course course);
Future<int> deleteCourse(int id);

// ---- 节次 ----
Future<List<Period>> getPeriods();                   // 按 idx 排序
Future<int> insertPeriod(Period period);
Future<int> updatePeriod(Period period);
Future<int> deletePeriod(int id);
Future<void> replacePeriods(List<Period> periods);   // 事务内整体替换（节次模板）

// ---- 停课/节假日 ----
Future<List<Holiday>> getHolidays({int? courseId, DateTime? from, DateTime? to});
Future<int> insertHoliday(Holiday holiday);
Future<int> updateHoliday(Holiday holiday);
Future<int> deleteHoliday(int id);
```

### 5.2 ITaskRepository

```dart
Future<List<Task>> getTasks({TaskType? type, bool? completed, DateTime? from, DateTime? to});
Future<Task?> getTaskById(int id);
Future<int> insertTask(Task task);                   // createdAt 为空自动填 now
Future<int> updateTask(Task task);
Future<int> deleteTask(int id);
Future<int> setCompleted(int id, bool completed);    // 完成时写 completedAt，取消时清空
```

### 5.3 ISettingsRepository

```dart
Future<String?> getValue(String key);
Future<void> setValue(String key, String value);     // upsert
Future<void> setAll(Map<String, String> entries);    // 批量 upsert（事务）
Future<Map<String, String>> getAll();
Future<void> remove(String key);
```

### 5.4 使用方式

feature 层在 `lib/features/**` 内通过 Riverpod Provider 组装，例如：

```dart
final timetableRepositoryProvider = Provider<ITimetableRepository>((ref) {
  return TimetableRepository(AppDatabase.instance);
});
```

页面 → `ref.watch(...)` → 接口方法 → 模型。**Provider 归属 feature 模块，不放在 `lib/data/`**。

---

## 6. 备份与恢复（.plai）

> 用途：本机完整备份（数据库 + 设置）。与课表 JSON/CSV（朋友间分享单学期）并存。

### 6.1 文件格式

`.plai` 为 UTF-8 格式化 JSON：

```json
{
  "format": "plai-backup",
  "version": 1,
  "exportedAt": "2026-08-31T12:00:00.000",
  "data": {
    "semesters": [{ "id": 1, "name": "2026 秋", "startDate": "2026-09-01", "totalWeeks": 16 }],
    "courses":   [{ "id": 1, "semesterId": 1, "name": "高等数学", "teacher": "", "location": "",
                     "color": "", "weekType": "every", "weekList": [],
                     "startWeek": 1, "endWeek": 16, "weekday": 1,
                     "startPeriod": 1, "endPeriod": 2 }],
    "periods":   [{ "id": 1, "index": 1, "startTime": "08:00", "endTime": "08:45" }],
    "holidays":  [{ "id": 1, "date": "2026-10-01", "courseId": null, "reason": "国庆" }],
    "tasks":     [{ "id": 1, "title": "交作业", "description": "", "type": "todo",
                     "dueDate": "2026-10-01", "dueTime": null, "priority": "normal",
                     "courseId": null, "remindOffsetMin": null, "remindDate": null,
                     "completed": false, "completedAt": null, "createdAt": "..." }],
    "settings":  { "remind_minutes": "10" }
  }
}
```

字段键与模型 `toJson()` 一致（camelCase）。`id` 在恢复时**不沿用**，由库重新自增；恢复过程自动重映射 `semesterId` / `courseId` 引用。

### 6.2 BackupService API

```dart
BackupService({
  required AppDatabase db,
  required ITimetableRepository timetable,
  required ITaskRepository tasks,
  required ISettingsRepository settings,
});

Future<Map<String, dynamic>> exportToJson();   // 当前库 → 备份 Map
Future<void> exportToFile(String path);        // 写 .plai 文件（UTF-8 缩进 JSON）
Future<Map<String, dynamic>> readFile(String path);   // 读 .plai 文件（不写库）
BackupPreview preview(Map<String, dynamic> json);     // 校验格式并返回摘要（非法抛 BackupFormatException）
Future<void> restore(Map<String, dynamic> json, {required RestoreStrategy strategy});

enum RestoreStrategy { overwrite, merge }

class BackupPreview { DateTime? exportedAt; int semesterCount; int courseCount;
                      int periodCount; int holidayCount; int taskCount; int settingCount; }
```

### 6.3 恢复策略

- **overwrite（覆盖）**：清空全部 6 表后整体写入。恢复前应强确认 + 二次输入确认（PRD §2）。
- **merge（合并）**：事务内并入，业务键去重：
  - 学期按 `name`；课程按 `(semesterId, name, weekday, startWeek, endWeek, startPeriod, endPeriod)`；节次按 `index`；停课按 `(date, courseId, reason)`；任务按 `(title, dueDate)`；设置「备份优先」覆盖。

---

## 7. 课表导入导出

> 用途：朋友间分享单学期课表（JSON/CSV）。导入**严格校验，任一错误整体失败，不部分写入**。

### 7.1 JSON 结构（PRD-设置与数据 §2）

```json
{
  "semester": { "name": "2026 秋", "startDate": "2026-09-01", "totalWeeks": 16 },
  "periods": [
    { "index": 1, "startTime": "08:00", "endTime": "08:45" },
    { "index": 2, "startTime": "08:55", "endTime": "09:40" }
  ],
  "courses": [
    { "name": "高等数学", "teacher": "王老师", "location": "教一101", "color": "#4C9AFF",
      "weekType": "every", "weekList": [], "startWeek": 1, "endWeek": 16,
      "weekday": 1, "startPeriod": 1, "endPeriod": 2 }
  ]
}
```

### 7.2 CSV 格式

表头（PRD 列序）与样例：

```
课程名,教师,地点,星期,开始节次,结束节次,周次类型,开始周,结束周,自定义周序列,颜色
高等数学,王老师,教一101,1,1,2,every,1,16,,#4C9AFF
英语,李老师,,3,3,4,custom,1,12,"1,3,5",#00FF00
```

- 必填列：`课程名 / 星期 / 开始节次 / 结束节次 / 周次类型 / 开始周 / 结束周`；可选列：`教师 / 地点 / 颜色 / 自定义周序列`。
- 周次类型接受 `every/odd/even/custom` 或中文 `每周/单周/双周/自定义`。
- 自定义周序列接受 `1,3,5`（CSV 单元格需加引号）或 `[1,3,5]`；`custom` 时必填非空。
- 颜色接受 `#RRGGBB` 或 `RRGGBB`，统一归一化为大写 `#RRGGBB`。
- 支持引号转义（`""`）、UTF-8 BOM。

### 7.3 校验规则（不满足即整体失败）

| 项目 | 规则 |
|---|---|
| 结构 | JSON 根为对象；含 `semester`/`periods`/`courses`；CSV 必填列齐全 |
| 学期 | name 非空；startDate `yyyy-MM-dd`；totalWeeks ≥ 1 |
| 节次 | index ≥ 1 且不重复；startTime/endTime 为 `HH:mm` 且 start < end |
| 课程 | 课程名非空；weekday 1–7；startWeek/endWeek 1–99 且 start ≤ end；startPeriod/endPeriod ≥ 1 且 start ≤ end；weekType 合法；custom 时 weekList 非空且每项 1–99；颜色格式合法 |

错误携带行号：JSON 为数组条目序号（1 起），CSV 为文件行号（1 起，含表头）。

### 7.4 API

```dart
TimetableImportExport({required AppDatabase db, required ITimetableRepository timetable});

// 导出
Future<String> exportJson(int semesterId);
Future<String> exportCsv(int semesterId);
Future<void> exportJsonToFile(int semesterId, String path);
Future<void> exportCsvToFile(int semesterId, String path);

// 导入（校验失败抛 TimetableImportException，整体不写入）
Future<TimetableImportResult> importJson(
  String content, {int? targetSemesterId, required ImportStrategy strategy});
Future<TimetableImportResult> importCsv(
  String content, {required int semesterId, required ImportStrategy strategy});
Future<TimetableImportResult> importJsonFromFile(
  String path, {int? targetSemesterId, required ImportStrategy strategy});
Future<TimetableImportResult> importCsvFromFile(
  String path, {required int semesterId, required ImportStrategy strategy});

enum ImportStrategy { overwrite, merge }
class TimetableImportResult { int semesterId; int periodsImported; int coursesImported; }
```

行为说明：
- `importJson` 的 `targetSemesterId == null` 时按文件内 `semester` **新建学期**并写入；否则写入指定学期（文件内 semester 仅结构校验，不更新现有学期）。
- `importCsv` 必须指定 `semesterId`（CSV 不含学期信息）；**不改变节次表**。
- `overwrite`：清空目标学期课程、整体替换节次表（JSON 含节次配置时）后写入。
- `merge`：新增缺失的课程/节次（课程按业务键去重，节次按 index 去重），不动已有数据。
- 内置节次模板见 `lib/data/db/default_periods.dart`（`defaultPeriods`，12 节），数据层不自动写入，由上层在节次表为空时插入。

---

## 8. 单元测试

- 位置：`test/data/`（`test_helpers.dart` 提供 `TestData.create()`：in-memory 库 + 三个 Repository）。
- 覆盖：schema 建表与外键级联、三组 Repository CRUD 往返、导入导出格式校验与往返、备份导出→覆盖恢复一致 / 合并去重。
- 运行：`flutter test test/data`（全量 `flutter test` 含骨架 widget_test）。
- 说明：`sqflite_common_ffi` 为 dev 依赖，仅宿主机测试用。因内网无法从 GitHub 下载 sqlite3 预编译库，`pubspec.yaml` 已配置 `hooks: user_defines: sqlite3: { source: system, name_windows: winsqlite3 }`，测试复用系统 `winsqlite3.dll`；该配置不影响 Android 构建（Android 走 sqflite 平台通道，不经过 sqlite3 包）。

---

## 9. 遗留问题与约定

- **V2 迁移**：新增 `point_log` / `ai_config` / `chat_session` / `chat_message` 时，`dbVersion` 升为 2，并在 `_onUpgrade` 补 `if (oldVersion < 2)` 建表；`db_schema.dart` 追加对应 DDL 常量。
- **周期/重复规则**（PRD V2）：task 目前仅一次性；V2 加重复字段需迁移。
- **holiday 日期段**：PRD 定义为单 `date`；日期段可用多条记录表达，V2 可按需扩展为起止日期。
- **`.plai` 预览**：`BackupService.preview` 仅做格式与数量校验；深度字段校验（非法枚举等）在 `restore` 解析时抛 `BackupFormatException`，建议 UI 恢复前先 `preview` 再 `restore`。
- **编码约定冲突**：以《编码约定.md》为准；本模块如需变更约定，先报主会话。
