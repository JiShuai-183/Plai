# Plai 编码约定（所有 Agent 必须阅读遵守）

> 由 plai-scaffold 产出。后续 feature agent（plai-data / plai-timetable / plai-schedule / plai-notify / plai-settings / plai-review）动工前**必须先读本文件**。
> 约定冲突时以本文件为准；如需变更，向主会话提出，先改文档再改代码。

## 0. 项目定位

- Plai 是面向学生的私人工具 App：**课表 + 日程 + 积分激励 + AI 辅助**，纯本地、零服务器，主打安卓（未来 Windows 桌面版）。
- V1 = 课表 / 日程 / 提醒（完全离线）；V2 = 积分激励 / AI / 桌面版。
- 技术栈：Flutter 3.47.x + Dart 3.13.x，Material 3，Riverpod 状态管理，SQLite（sqflite）本地库。

## 1. 目录结构（严格遵循，不得另起炉灶）

```
lib/
  main.dart            # 入口：ProviderScope + MaterialApp（已建）
  app_shell.dart       # 底部导航（课表/今日/设置）（已建）
  theme/               # 主题：colors.dart / theme.dart（M3 + 深色）
  routes/              # 路由表：app_routes.dart（常量） / route_registry.dart（页面登记）
  data/                # plai-data 专属，其它模块只读其接口，禁止直接写库
    db/                #   sqflite 数据库连接与迁移
    models/            #   领域模型（Course、Task 等）
    repositories/      #   Repository 接口与实现（feature 只依赖接口）
    backup/            #   备份/恢复（.plai 文件）
    import_export/     #   课表 JSON/CSV 导入导出
  features/
    timetable/         # plai-timetable 专属
    schedule/          # plai-schedule 专属
    settings/          # plai-settings 专属
  services/
    notifications/     # plai-notify 专属（flutter_local_notifications）
  shared/              # 通用组件（空态视图、确认对话框等），各模块可复用
```

- 归属原则：**文件只允许由对应 agent 创建/修改**（见下表）。feature agent 改文件前先确认归属，防止 agent 间打架。

| 目录 | 归属 agent | 其它 agent 可做 |
|---|---|---|
| lib/data/** | plai-data | 只读、import 接口 |
| lib/features/timetable/** | plai-timetable | 只读 |
| lib/features/schedule/** | plai-schedule | 只读 |
| lib/features/settings/** | plai-settings | 只读 |
| lib/services/notifications/** | plai-notify | 调用 notify 提供的方法 |
| lib/theme/ lib/routes/ lib/shared/ lib/main.dart lib/app_shell.dart | plai-scaffold（骨架） | 需要改动先经主会话确认 |

## 2. 命名规范

| 对象 | 规范 | 示例 |
|---|---|---|
| 文件 | snake_case，一个类一个文件，页面后缀 `_page.dart` | `timetable_page.dart`、`app_shell.dart` |
| 类（Widget/Model） | UpperCamelCase | `TimetablePage`、`Course` |
| 抽象/接口 | UpperCamelCase，Repository 前缀 `I` | `ICourseRepository` |
| 变量/函数 | lowerCamelCase | `courseList`、`fetchCourses()` |
| 常量 | `static const` 用 UpperCamelCase；局部 `final` 用 lowerCamelCase | `AppRoutes.timetable`、`PlaiColors.seed` |
| 枚举 | 类型 UpperCamelCase，成员 lowerCamelCase | `enum WeekType { single, double, both }` |
| Riverpod Provider | 全局 `final xxxProvider`，lowerCamelCase | `courseListProvider` |
| 路由常量 | 见 §5 | `AppRoutes.timetable` |
| 数据库表/字段 | snake_case | `timetable_courses`、`week_type` |

## 3. Riverpod 用法（flutter_riverpod 2.x）

- 应用根在 `main.dart` 已包 `ProviderScope`，新增全局 Provider 直接定义即可，**不要重复嵌套 ProviderScope**。
- 数据流：feature 页面的 `ConsumerWidget` / `ConsumerStatefulWidget` → 通过 Provider 读 `Repository` → 返回模型。页面**禁止**直接操作数据库。
- 只读数据用 `FutureProvider` / `StreamProvider`；本地可变状态（表单、选中项）用 `StateProvider` 或 `NotifierProvider`，能放 widget 本地 `State` 的就不要全局。
- Provider 定义位置：归属于模块的 Provider 放**该模块**内（如 `lib/features/timetable/` 下），不放 `lib/data/`；数据层只提供 Repository 接口，由 feature 层包 Provider 组装。
- 抛错与加载：页面统一处理 `AsyncValue` 的 `loading / error / data` 三态，空态用 `lib/shared/` 的通用视图。

## 4. Repository 使用方式（数据层纪律）

- **feature agent 只允许 import `lib/data/` 暴露的模型与 Repository 接口，禁止直接写 SQLite、禁止 import `sqflite`**。
- 一切持久化读写必须经过 `lib/data/repositories/` 的接口；接口与实现由 plai-data 定义并实现，feature 依赖接口（`ICourseRepository` 等）以便后续可替换/测试。
- 数据库 schema、迁移、备份、导入导出全部归 plai-data，feature 不得自行建表。
- 若 feature 发现数据层缺接口，回报 plai-data（或主会话协调），由 plai-data 补，feature 不得绕过。

## 5. 路由与导航

- 所有跳转使用命名路由，常量集中在 `lib/routes/app_routes.dart`（`AppRoutes`）。
- 新页面两处登记：`app_routes.dart` 补常量 → `lib/routes/route_registry.dart` 的 `routeRegistry` 表登记构造器。
- `AppRoutes.root` 是底部导航外壳（`AppShell`），feature 页作为 Tab 内容由 `AppShell` 的 `IndexedStack` 承载，**不要把 Tab 页 push 成新路由**；详情页才用命名路由 push。
- 跳转示例：`Navigator.pushNamed(context, AppRoutes.settings)`。

## 6. 主题与颜色

- Material 3，亮/暗主题随系统切换（`ThemeMode.system`，已在 `main.dart`）。
- 颜色统一引用 `lib/theme/colors.dart`（`PlaiColors`）或从 `Theme.of(context).colorScheme` 取；**禁止硬编码色值**。新增颜色先登记 `PlaiColors` 再使用。
- 积分激励配色语义（V2）：高分深绿 `PlaiColors.deepGreen`、低分浅绿 `PlaiColors.lightGreen`、未达标灰 `PlaiColors.mutedGrey`。

## 7. 日期/周次与格式化

- 用 `intl` 做日期与周次格式化，统一集中一个格式化工具（可放 `lib/shared/`），页面不散落 `DateFormat`。
- 周次规则引擎归 plai-timetable（单双周/周序/节假日），实现成独立纯逻辑类，便于单测。

## 8. 通知（plai-notify）

- 通知统一封装在 `lib/services/notifications/`，对外暴露**方法签名**（如「某课程/任务在何时提醒」），由 plai-notify 定；timetable / schedule 只调用接口，不直接碰 `flutter_local_notifications`。
- 权限申请与国内 ROM 保活引导由 plai-notify 负责，feature 只触发入口。

## 9. 测试

- 纯逻辑（周次引擎、格式化、Repository）写单测，放 `test/`；页面写 `widget_test`。
- 提交前必须 `flutter analyze` 无 error；涉及逻辑变更尽量补测试。

## 10. 代码质量与协作纪律

- 全部文件使用 UTF-8；中文文案直接写中文字符串，不转义。
- 不输出 `print`/`debugPrint` 到业务路径；临时调试用 `// TODO(agent): 删除调试` 标注。
- **不修改** `.claude/` 下任何文件（agent 定义与协作说明归主会话）。
- 每个 agent 完成后回报：完成/未完成 + 文件清单 + 遗留问题；跨模块问题交主会话协调，不在自己模块内越权实现。

## 11. 工作流程：任务分模块，每模块完成即 GIT（主会话 / Claude 与各 agent 共同遵守）

- 任何任务**先拆成小模块**，一次只做一个模块，不跨模块混做；模块边界沿用 §1 目录归属表与 plai-* agent 职责。
- 模块完成判据：**实现完毕 + 相关测试通过 + `flutter analyze` 无告警**；改动涉及全仓库可见行为时跑 `flutter test` 全量回归。
- 达标后**立即单独 commit 一次**，不攒到多个模块合并提交；未完成模块不得堆积成半成品提交。
- Commit message 用中文 + 语义化前缀（`feat:` / `fix:` / `perf:` / `refactor:` / `docs:` / `test:` / `style:` / `chore:`），与 git 历史一致；正文按需补要点，一行一意。
- 构建产物（APK 等）不进 git；仅在用户明确要求时才 commit（本规范约定的「每模块一 commit」本身即视为已授权提交本次改动的代码与测试文件）。

## 12. AI 模块速查（后续 AI 改动先看这里）

- 直连 OpenAI 兼容端点统一经 `lib/services/ai/llm_client.dart` 的 `LlmClient`；瞬时繁忙 HTTP 429/503 已内置指数退避自动重试（默认 2 次），**不要在页面层放大定时器**——会卡 widget 测试的 `pumpAndSettle` 假时钟。
- 请求默认**不携带** `temperature`（思考/推理型模型只允许默认采样，各厂商约束不一）；新功能不要写死温度，确需低温再显式传。
- 工具 schema 一律经 `functionTool()` 生成：缺顶层 `type:"object"` 会自动补齐为完整 JSON Schema——**不要手写裸空 `{}` parameters**（Moonshot / kimi 等严格服务商会 400）。
- 工具轮前言文字（如"我将调用 xx 函数"）不入库、不进 wire，用户只看到结果；改动 AI 系统提示词后跑 S6–S10 相关 widget 测试。
- release APK 产物命名 `build/app/outputs/flutter-apk/Plai.<version>.apk`，versionCode 递增保证覆盖安装。
